using System;

namespace MakingProgress
{
  class Program
  {
    static void Main(string[] args)
    {
      // declare steps variable
      int steps = 0;
      steps += 2;
      steps -= 1;
  
      Console.WriteLine(steps);
      

    }
  }
}

